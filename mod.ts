/**
 * A haphazard collection of tools I keep reaching for in
 * TypeScript, put together in one place so they're easy for me to find.
 * 
 * @module
 */

export { Mutex } from "./src/mutex.ts"
export { lazy, range } from "./src/iterators.ts"